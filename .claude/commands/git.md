---
name: git
description: Enforce conventional git commit messages — single-line typed format
---

## Usage

```
/git
```

Reads the staged diff and proposes a commit message in the format `<type>: <short description>`.

Full convention, type table, examples, and rules live in `.claude/skills/git/SKILL.md`. Read that file and follow the workflow exactly.
